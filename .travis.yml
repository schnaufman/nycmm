language: node_js

node_js:
  - '14'

before_install:
- rvm install 3.0.1p64
- curl -o- -L https://yarnpkg.com/install.sh | bash

before_script:
  # install dependencies
  # travis runs `yarn install` when yarn.lock exists

  # install ruby dependencies
  - gem install bundler
  - bundle install
  - yarn build
